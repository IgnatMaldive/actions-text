name: CI
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Commit new file
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "Add hackernews.md"